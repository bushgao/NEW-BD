# 联系方式采集功能使用指南

## 快速开始

### 方法一：使用 Chrome 扩展（推荐）

1. **重新加载扩展**
   - 打开 `chrome://extensions/`
   - 找到 "Zilo 达人采集助手"
   - 点击刷新按钮 🔄

2. **访问达人详情页**
   - 登录抖音精选联盟
   - 进入任意达人详情页
   - URL 示例: `https://buyin.jinritemai.com/dashboard/servicehall/daren-profile?ewid=xxx`

3. **采集达人信息**
   - 点击页面上的"添加到 Zilo"按钮
   - 等待采集完成
   - 查看成功提示

4. **验证数据**
   - 登录 Zilo 系统: http://localhost:5173
   - 进入"达人管理"
   - 查看新添加的达人
   - 确认手机号/微信号字段

### 方法二：使用测试脚本（调试用）

1. **打开达人详情页**
   - 确保 URL 包含 `ewid` 参数

2. **打开浏览器控制台**
   - 按 F12 或右键 → 检查
   - 切换到 Console 标签

3. **复制并运行测试脚本**
   ```javascript
   // 复制 chrome-extension/测试联系方式采集.js 的内容
   // 粘贴到控制台并按回车
   ```

4. **查看测试结果**
   - 控制台会显示详细的采集过程
   - 包括 API 响应、分类逻辑、最终结果

## 联系方式分类规则

### 自动分类为"手机号"
- ✅ 包含特殊字符: `15784254551(+8081)`
- ✅ 纯数字且长度 ≥11: `13800138000`
- ✅ 无法明确分类时的默认选项

### 自动分类为"微信号"
- ✅ 包含字母: `zzcdb2121`
- ✅ 字母数字组合: `wechat_abc123`

### 需要手动调整的情况
- ⚠️ 纯数字的微信号（如 `12345678901`）会被误判为手机号
- ⚠️ 建议在前端 UI 中添加手动编辑功能

## 常见问题

### Q1: 为什么联系方式字段为空？

**可能原因：**
1. 达人未公开联系方式
2. 你的账号没有查看权限
3. API 请求失败（网络问题、登录过期）

**解决方法：**
- 检查浏览器控制台的错误信息
- 确认已登录抖音精选联盟
- 尝试手动点击"眼睛"图标查看联系方式

### Q2: 手机号和微信号分类错误怎么办？

**临时方案：**
- 在 Zilo 系统中手动编辑达人信息
- 调整手机号和微信号字段

**长期方案：**
- 前端 UI 添加字段编辑功能
- 添加字段验证和格式检查

### Q3: 如何查看采集日志？

**Chrome 扩展日志：**
1. 打开 `chrome://extensions/`
2. 找到 "Zilo 达人采集助手"
3. 点击"检查视图" → "service worker"
4. 查看 Console 标签

**页面日志：**
1. 在达人详情页按 F12
2. 切换到 Console 标签
3. 查找 `[Zilo]` 开头的日志

### Q4: API 返回 403 或 401 错误？

**原因：**
- 登录已过期
- 没有查看权限

**解决：**
1. 重新登录抖音精选联盟
2. 刷新达人详情页
3. 重试采集

## 调试技巧

### 1. 查看 API 请求
```javascript
// 在控制台运行
const ewid = new URLSearchParams(window.location.search).get('ewid');
console.log('ewid:', ewid);

fetch(`https://buyin.jinritemai.com/api/contact/contact_info?ewid=${ewid}`, {
  credentials: 'include'
})
.then(r => r.json())
.then(d => console.log('API 响应:', d));
```

### 2. 测试分类逻辑
```javascript
function classifyContact(value) {
  const hasSpecialChars = /[()+-]/.test(value);
  const isAllDigits = /^\d+$/.test(value);
  const hasLetters = /[a-zA-Z]/.test(value);
  
  if (hasSpecialChars || (isAllDigits && value.length >= 11)) {
    return 'phone';
  } else if (hasLetters) {
    return 'wechat';
  } else {
    return 'phone (default)';
  }
}

// 测试
console.log(classifyContact('15784254551(+8081)')); // phone
console.log(classifyContact('zzcdb2121'));          // wechat
console.log(classifyContact('13800138000'));        // phone
console.log(classifyContact('12345678901'));        // phone (default)
```

### 3. 模拟完整采集流程
```javascript
// 运行 chrome-extension/测试联系方式采集.js
// 查看完整的采集过程和结果
```

## 数据示例

### 成功采集示例
```json
{
  "nickname": "美妆达人小红",
  "platform": "DOUYIN",
  "platformId": "dy123456",
  "phone": "13800138000",
  "wechat": "",
  "followers": "50000",
  "categories": ["美妆"],
  "tags": ["LV5", "性别:女", "地区:北京·朝阳"],
  "notes": "头像: https://...\n通过浏览器插件采集"
}
```

### 微信号示例
```json
{
  "nickname": "时尚博主",
  "platform": "DOUYIN",
  "platformId": "dy789012",
  "phone": "",
  "wechat": "fashion_blogger",
  "followers": "100000",
  "categories": ["服饰"],
  "tags": ["LV6"],
  "notes": "通过浏览器插件采集"
}
```

## 安全提示

⚠️ **隐私保护**
- 联系方式是敏感信息，请妥善保管
- 不要将联系方式分享给未授权人员
- 遵守相关隐私法规和平台规则

⚠️ **数据使用**
- 仅用于合法的商务合作
- 不得用于骚扰或垃圾信息
- 尊重达人的隐私权

## 技术支持

如有问题，请查看：
- `chrome-extension/联系方式采集功能完成.md` - 技术实现细节
- `chrome-extension/测试联系方式采集.js` - 测试脚本
- 浏览器控制台日志 - 实时调试信息

---

**更新时间**: 2026-01-06
**版本**: v1.1.0

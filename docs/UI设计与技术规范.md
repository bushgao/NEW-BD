# 🎨 NEW BD UI 设计与技术规范文档

> **文档状态**: ✅ 已完成全局标准化  
> **最后更新**: 2026-01-12  
> **视觉风格**: Bento Glassmorphism (高端毛玻璃便当风格)

---

## 1. 核心设计理念

项目采用 **Bento Glassmorphism** (便当盒毛玻璃) 风格，旨在打造一个专业、现代且具有“指挥舱”质感的企业级管理系统。

- **模块化 (Bento Layout)**: 所有的内容都承载在独立的、圆角巨大的卡片（Bento Cards）中。
- **透明感 (Glassmorphism)**: 卡片采用半透明磨砂质感，结合背景微弱的色彩流动感（Aurora Background）。
- **紧凑性 (Compactness)**: 针对管理系统的信息密度需求，优化了组件间距和高度，确保首屏信息量。

---

## 2. 布局标准 (Layout Standards)

### 2.1 页面容器
所有核心业务页面（Dashboard, Admin, Influencer Portal）必须遵循统一的容器规范：
- **最小高度**: `min-height: 100vh`
- **内边距**: `padding: 40px`
- **外边距**: `margin: -24px` (用于抵消 Layout 默认边距，实现全屏沉浸感)
- **背景**: 渐变背景 + Aurora 装饰元素

### 2.2 Bento 网格系统
使用自定义 `BentoGrid` 组件：
- **间隙 (Gap)**: `24px` (`gap-6`)
- **标准行高 (Row Height)**: `gridAutoRows: minmax(110px, auto)`
- **自适应**: 移动端降级为单列布局

---

## 3. 组件规范 (Component Specs)

### 3.1 BentoCard (核心卡片)
`.bento-card` 类定义在 `index.css` 中，核心参数如下：
- **背景色**: `rgba(255, 255, 255, 0.4)`
- **模糊度 (Backdrop Blur)**: `12px`
- **边框**: `1px solid rgba(255, 255, 255, 0.4)`
- **圆角 (Radius)**: `1.5rem` (24px, 对应 `3xl` 变体)
- **阴影**: Subtle shadow with a hint of depth
- **交互**: Hover 时 `translateY(-4px)`，背景透明度提升至 `0.6`

### 3.2 顶部统计行 (Row 1)
为了实现“指挥舱”效果，页面第一行统计卡片需使用：
- **`tight={true}`**: 压缩 Header 下边距
- **`padding="16px 20px"`**: 更加紧凑的内部间距
- **内容**: 建议使用 `Flex` 布局，数字与标签居中对齐

---

## 4. 视觉元素 (Visual Elements)

### 4.1 极光背景 (Aurora Background)
通过在页面顶部插入两个模糊的圆形 `div` 实现：
- **左上角**: `width: 400px`, `height: 400px`, `filter: blur(80px)`, `rgba(90, 200, 250, 0.08)` (蓝色系)
- **右下角**: `width: 500px`, `height: 500px`, `filter: blur(100px)`, `rgba(255, 107, 107, 0.08)` (粉色系)

### 4.2 文字与排版
- **字体**: `Inter`, `SF Pro`, `Helvetica Neue`, `sans-serif`
- **层次**:
  - 标题: `font-semibold`, 渐变文字效果 (`text-gradient`)
  - 数据: `font-bold`, 大号字号

---

## 5. 颜色令牌 (Color Tokens)

| 类别 | 描述 | 十六进制 (Hex) |
|------|------|---------------|
| **Primary** | 品牌主色 | `#0c8ce9` |
| **Secondary** | 背景浅灰色 | `#f5f7fa` |
| **Success** | 成功/盈利 | `#34c759` |
| **Error** | 错误/亏损 | `#ff3b30` |
| **Warning** | 警告/延迟 | `#ff9f0a` |

---

## 6. 技术实现参考

### CSS 类名 (Tailwind)
```css
/* Bento 卡片基础类 */
.bento-card {
  @apply bg-white/40 backdrop-blur-xl border border-white/40 rounded-3xl shadow-sm transition-all duration-400;
}
```

### React 组件调用
```tsx
<BentoCard 
  title="实时数据" 
  icon={<ChartIcon />} 
  tight={true} 
  padding="16px 20px"
>
  {/* 内容 */}
</BentoCard>
```

---

## 📝 维护记录
- **2026-01-10**: 初步确立 Bento 风格并应用于主 Dashboard。
- **2026-01-11**: 统一全局内边距规范（40px/-24px）。
- **2026-01-12**: 完成 Admin/Influencer 全门户 UI 标化，并整理此规范文档。
